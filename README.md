# Reason Proto

```shell
# normal add
git submodule add -b master --name api/auth https://github.com/go-cinch/auth-proto.git ./api/auth

# or force add
git submodule add -f -b master --name api/auth https://github.com/go-cinch/auth-proto.git ./api/auth
```